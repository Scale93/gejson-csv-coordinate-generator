#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../random-points-generator/bin/random-points-generator" "$@"
  ret=$?
else 
  node  "$basedir/../random-points-generator/bin/random-points-generator" "$@"
  ret=$?
fi
exit $ret
